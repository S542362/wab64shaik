extends layout

block content
  h1= title
  p Welcome to #{title}
  p you can be afraid and brave at the same time.
  button(type="button",onclick="update()" class="btn btn-dark") Press Me!!
  div(id="target") clickme
  script.
    var client_count=0;
    function update() {
      client_count++;
      document.getElementById("target").innerHTML=`Value now ${client_count}`
    }
     a(href="/horses") go to horses
  
